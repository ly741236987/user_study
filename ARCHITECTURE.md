# 架构说明（AI 股票看板 MVP）

## 1. 目标

构建一个“可运行、可迭代”的股票看板原型，先打通：
1. 行情数据输入
2. AI 信号评分输入
3. 建议引擎输出（买入/卖出/观望 + 数量）
4. 可视化展示

## 2. 分层设计

- **数据层**：`providers.py`
  - 定义 `StockProvider`
  - 当前实现 `MockStockProvider`
  - 后续可接入东方财富/同花顺/券商等合规数据接口

- **策略层**：`engine.py`
  - `RecommendationEngine`
  - 输入：价格、信号分、现金、持仓
  - 输出：动作 + 数量 + 原因

- **模型层**：`models.py`
  - `StockSnapshot`：行情快照
  - `TradeSuggestion`：交易建议

- **展示层**：`app.py`
  - Streamlit 看板
  - 显示关键字段与建议汇总

## 3. 当前策略逻辑（MVP）

- `signal_score >= buy_threshold`：尝试买入
- `signal_score <= sell_threshold` 且有持仓：尝试卖出
- 其他：观望

数量规则：
- 买入：`可用现金 * 单票最大仓位占比`，按 100 股整数倍计算
- 卖出：优先卖出一半持仓（按 100 股整数倍）

## 4. 下一步演进建议

1. **数据接入**：引入真实行情和基本面数据
2. **模型升级**：把规则信号改为多因子/LLM+特征模型
3. **风控模块**：止损、止盈、最大回撤、行业集中度
4. **回测模块**：历史回放 + 手续费滑点
5. **执行模块**：人工确认后再下单，不建议全自动

## 5. 合规与安全

- 遵守数据源平台协议、API 授权和本地法律法规。
- 输出需明确“非投资建议”，并保留人工确认环节。
